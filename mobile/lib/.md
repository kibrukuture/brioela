├── lib/
│ ├── api.ts # A central API client setup (e.g., Axios instance)
│ ├── case-converter.ts # The same utility to convert API response keys to camelCase
│ └── utils.ts # General utility functions (e.g., date formatting)
