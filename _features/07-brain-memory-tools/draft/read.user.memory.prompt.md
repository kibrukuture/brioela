# Draft: read.user.memory.prompt.ts

Target: `backend/src/agents/brain/_tools/_prompts/read.user.memory.prompt.ts`

```typescript
export const readUserMemoryPrompt = 'Read a specific fact or a full namespace from user_memory mid-session. Use only when context is not already loaded in prompt.'
```
