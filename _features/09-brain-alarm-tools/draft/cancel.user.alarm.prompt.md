# Draft: cancel.user.alarm.prompt

Target: `backend/src/agents/brain/_tools/_prompts/cancel.user.alarm.prompt.ts`

```typescript
export const cancelUserAlarmPrompt =
	'Cancel a pending alarm by id. Only pending alarms can be cancelled. The row is kept with status cancelled for audit history.'
```
