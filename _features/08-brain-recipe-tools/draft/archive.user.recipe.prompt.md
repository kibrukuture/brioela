# Draft: archive.user.recipe.prompt

Target: `backend/src/agents/brain/_tools/_prompts/archive.user.recipe.prompt.ts`

```typescript
export const archiveUserRecipePrompt = 'Archive a recipe so it leaves the active index. Row and content are preserved. Use only on explicit user request or clear obsolescence.'
```
