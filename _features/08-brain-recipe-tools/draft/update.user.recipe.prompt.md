# Draft: update.user.recipe.prompt

Target: `backend/src/agents/brain/_tools/_prompts/update.user.recipe.prompt.ts`

```typescript
export const updateUserRecipePrompt = 'Refine an existing recipe after a cooking session. Archives the previous version before overwriting. Use only for real post-session learnings.'
```
