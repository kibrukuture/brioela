# Draft: view.user.recipe.prompt

Target: `backend/src/agents/brain/_tools/_prompts/view.user.recipe.prompt.ts`

```typescript
export const viewUserRecipePrompt = 'Load full recipe content on demand. Call only when you need ingredients, steps, or cookable detail for a specific recipe id from the index.'
```
